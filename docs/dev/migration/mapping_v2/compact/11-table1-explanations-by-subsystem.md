# Table 1 Explanations By Subsystem

Scope: explanations for all non-`Exact` rows in
`01-matlab-called-functions-to-python-branches-compact.md`.

## Registration, atlas alignment, and IO setup

| Row key | Purpose | Current coverage | Missing behavior | Why it matters | Recommended action | Priority | Confidence |
|---|---|---|---|---|---|---|---|
| `TS_fnc_ASEGAtlasRegistration` | Segmentation-guided atlas/mesh registration stage. | `dev` and `dti_integration` use `MeshModel.fit_mesh_to_seg`. | MATLAB helper returns richer intermediate state and options. | Incomplete parity during initialization can shift downstream fitting. | Add a dedicated helper in `thalamus.py`/`thalamusDTI.py` to mirror MATLAB registration options and outputs. | `P1` | `Medium` |
| `TS_fnc_AffineImageDumpRegistration` | Affine alignment for atlas/image setup. | Branches use `align_atlas_to_seg` with similar intent. | Missing MATLAB-specific interface and saved transform artifacts. | Affine differences can bias crop and prior placement. | Add explicit affine stage object and optional transform dumps in `core.py`. | `P1` | `Medium` |
| `TS_fnc_GetKVLimageShifts` | Computes voxel-space shift bookkeeping for KVL images. | No direct `dev` equivalent; nearest partial in `thalamusDTI.py` preprocessing. | Shift helper not explicitly represented in Python API. | Hidden shift errors are hard to debug in multimodal runs. | Introduce explicit shift metadata capture in preprocessing return values. | `P1` | `Low` |
| `TS_fnc_mergeASEGwithAtlas` | Harmonizes coarse segmentation labels with atlas classes. | Partial in `thalamus.py` and `thalamusDTI.py` preprocessing. | MATLAB merge rules are more complete and configurable. | Label merge mismatches affect cheating fit and priors. | Externalize remap table and run parity checks against MATLAB label sets. | `P1` | `Medium` |
| `TS_fnc_mergeSYNTHSEGwithAtlas` | Fallback merge path for SynthSeg-derived coarse labels. | No `dev` equivalent; partial prototype in `thalamusDTI.py`. | No production SynthSeg merge pipeline in stable path. | Blocks robust operation when ASEG is unavailable. | Add configurable coarse-label fallback order and merge utility in `thalamus.py`. | `P0` | `Low` |
| `TS_fnc_readDTIintoGEMS` | Loads DTI-derived channels into fitting pipeline. | No `dev` equivalent; partial ingestion in `thalamusDTI.py`. | Incomplete DTI tensor/channel ingestion and validation. | Core blocker for dMRI parity. | Implement a typed DTI input loader and channel registry in `thalamusDTI.py`. | `P0` | `Medium` |
| `TS_fnc_resampleDTIfiles` | Resamples DTI files into segmentation working space. | No `dev` equivalent; partial preprocessing workaround in `thalamusDTI.py`. | Dedicated DTI resample stage and metadata tracking absent. | Spatial mismatch can invalidate joint likelihoods. | Add deterministic resampling helper with affines and provenance checks. | `P0` | `Low` |
| `kvlReadCroppedImage` | Reads/crops images using atlas ROI. | Partial in both branches via `sf.load_volume` and crop logic. | No explicit wrapper parity for all MATLAB behaviors. | Crop parity affects optimization speed and robustness. | Centralize crop API in `core.py` with explicit ROI and mask outputs. | `P1` | `Medium` |
| `kvlSetImageBuffer` | Updates image buffer for KVL calculators. | Partial via numpy buffer mutation and `KvlImage`. | Buffer lifecycle semantics are implicit in Python. | Implicit mutation can create subtle state bugs. | Add a small wrapper in `core.py` for buffer set/get with assertions. | `P2` | `Medium` |
| `kvlClear` | Explicitly frees KVL objects/resources in MATLAB flow. | Partial lifecycle cleanup in Python object teardown. | No explicit clear semantics per stage. | Long runs can leak memory/state if lifecycle is unclear. | Add explicit cleanup calls at stage boundaries and document ownership. | `P2` | `Low` |

## Statistical modeling and priors

| Row key | Purpose | Current coverage | Missing behavior | Why it matters | Recommended action | Priority | Confidence |
|---|---|---|---|---|---|---|---|
| `TS_fnc_fitGaussianHyperParams` | Estimates class/group Gaussian hyperparameters. | Partial equivalents in `thalamus.py` and `thalamusDTI.py`. | Python path remains structurally simpler than updated MATLAB. | Hyperparameter mismatch shifts class posteriors. | Expand channel-aware estimation and add parity tests for grouped classes. | `P1` | `High` |
| `TS_fnc_fitGaussian_withprior` | Fits Gaussian parameters with prior regularization. | Partial in `core.py` EM updates. | Exact prior formulation differs from MATLAB helper. | Prior mismatch affects stability in low-signal regions. | Refactor EM prior term construction into dedicated helper for comparability. | `P1` | `High` |
| `TS_fnc_gaussianPDF` | Gaussian likelihood evaluation primitive. | Partial semantic parity in fitting/extraction stages. | No direct standalone helper parity interface. | Harder to validate numeric parity by component. | Add optional debug hook to emit per-class likelihood terms. | `P2` | `High` |
| `TS_fnc_gaussprior_NIW` | Normal-inverse-Wishart prior contribution helper. | Partial simplified priors in Python. | No explicit NIW formulation in current code paths. | Limits full parity with updated Bayesian formulation. | Introduce NIW-compatible prior mode behind explicit option flag. | `P0` | `Medium` |
| `TS_fnc_getWeightSpacePosteriors` | Converts posteriors across grouped/weight spaces. | Partial overlap in `extract_segmentation`. | Missing explicit weight-space transform tooling. | Prevents direct parity checks for grouped output stages. | Implement optional transform utility and expose debug exports. | `P1` | `Medium` |
| `TS_fnc_checkForDisappearingClass` | Guard against vanishing class assignments during EM. | No equivalent in either branch. | Missing failure safeguard and recovery behavior. | EM can collapse class estimates silently. | Add disappearing-class detector with fallback prior inflation. | `P0` | `None` |
| `TS_fnc_splitClassSeedPrior` | Splits class seed priors for grouped initialization. | No equivalent in either branch. | Seed prior split logic absent. | Weak initialization for grouped models and bimodal classes. | Implement split-prior utility in `utils.py` and wire to group setup. | `P1` | `None` |
| `TS_fnc_erfireplacement` | Numerical approximation helper used by tensor likelihood path. | No equivalent in either branch. | Approximation function and call chain missing. | Needed for stable diffusion likelihood numerics. | Add helper only when tensor-likelihood path is introduced. | `P2` | `None` |
| `TS_fnc_evaluateTensorLikelihood` | Evaluates diffusion/tensor likelihood term. | No `dev` equivalent; low-confidence partial plumbing in `thalamusDTI.py`. | No complete tensor likelihood evaluator implementation. | Major parity blocker for diffusion-enabled method. | Define tensor likelihood interface in `core.py` and implement in `thalamusDTI.py`. | `P0` | `Low` |
| `TS_fnc_fitTensorDistribution` | Fits tensor distribution parameters per class/group. | No equivalent in either branch. | Tensor parameter fitting stage absent. | Joint sMRI+dMRI model cannot be completed without this stage. | Add tensor-parameter fit step after structural initialization. | `P0` | `None` |
| `TS_fnc_fitTensorReflection` | Reflection-aware tensor fitting refinement. | No equivalent in either branch. | Reflection term and controls absent. | Potential asymmetry/robustness gap in diffusion modeling. | Add as optional advanced mode after baseline tensor fit is stable. | `P2` | `None` |
| `TS_fnc_fitTensorReflection_initial` | Initial reflection-aware tensor setup. | No equivalent in either branch. | Initialization helper absent. | Missing initialization path can reduce convergence quality. | Add only after main tensor likelihood path exists. | `P2` | `None` |

## Label grouping and label-space transforms

| Row key | Purpose | Current coverage | Missing behavior | Why it matters | Recommended action | Priority | Confidence |
|---|---|---|---|---|---|---|---|
| `TS_fnc_groupSorting` | Builds ordered label groups and transforms. | Partial in `thalamus.py`; partial in `thalamusDTI.py`. | MATLAB grouping transform richness not fully replicated. | Group definitions drive priors, means, and outputs. | Unify grouping schema (including transforms) in `utils.py` and consume in both models. | `P0` | `Medium` |
| `TS_fnc_group_compression_read` | Reads compression CLUT and group mapping. | Partial parity in both branches through CLUT helpers. | Some MATLAB grouping metadata not surfaced in Python. | Compression mismatch impacts posterior aggregation and labels. | Extend helper return contract to include full grouping metadata. | `P1` | `High` |
| `TS_fnc_convertGIF2ASEG` | Converts GIF segmentation labels to ASEG namespace. | No equivalent in either branch. | Conversion utility absent. | Blocks portability for GIF-based datasets. | Add optional pre-conversion utility in CLI preprocessing stage. | `P2` | `None` |
| `TS_fnc_flipAseg` | Left-right ASEG flip helper used in alignment workflows. | No equivalent in either branch. | Explicit flip operation absent. | Needed for some registration/augmentation parity paths. | Add tested label-flip utility with strict hemisphere mapping table. | `P2` | `None` |

## Optimization orchestration and outputs

| Row key | Purpose | Current coverage | Missing behavior | Why it matters | Recommended action | Priority | Confidence |
|---|---|---|---|---|---|---|---|
| `TS_fnc_constructMeshDeformationCalculator` | Constructs objective calculator for deformation iterations. | Partial equivalent in `core.py` objective setup. | Updated MATLAB supports richer modality-dependent objective variants. | Objective construction is the center of parity for DTI integration. | Refactor objective assembly into explicit strategy objects (structural vs joint). | `P0` | `High` |
| `TS_fnc_OutputVolumeMeasurements` | Computes and writes grouped/nuclei volume summaries. | Partial in `thalamus.py` and mesh write routines. | MATLAB helper has richer grouped measurement outputs. | Output parity is needed for downstream comparisons. | Extend postprocess writer with grouped outputs and stable schema. | `P1` | `High` |
| `TS_fnc_outputProbVolumes` | Writes posterior/probability volume diagnostics. | No `dev` equivalent; limited prototype outputs in `thalamusDTI.py`. | General posterior export utility is missing. | Limits QC and parity validation during migration. | Add optional posterior/likelihood dump switches in model settings. | `P1` | `Low` |
| `myMRIwrite` | Generic MRI write helper with datatype/tempdir controls. | Partial equivalents in `Volume.save` and volume writers. | Not all MATLAB write options and staging behaviors are exposed. | Output metadata mismatches can complicate regression checks. | Add explicit writer options where parity is required; keep defaults simple. | `P2` | `High` |
